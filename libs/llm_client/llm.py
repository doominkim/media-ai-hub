def summarize(text: str) -> str:
    # 실제 LLM 연동 전용 stub
    return f"dummy summary for: {text[:20]}..."

def analyze_sentiment(text: str) -> str:
    # 실제 LLM 감정 분석 stub
    return f"dummy sentiment for: {text[:20]}..." 